# Introduction

The repo contains several components:

* RESTful API server
* RMT Library (Server Side)
* RMT Engine (Client Side)

# Build

* Build RMT_core

```
cd RMT_core
cmake -Bbuild -H.
cmake --build build
```
